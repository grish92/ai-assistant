[project]
name = "ai-assistant"
version = "0.1.0"
description = "AI Assistant"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]